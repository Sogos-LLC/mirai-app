apiVersion: v1
kind: Service
metadata:
  name: mirai-diagram
  namespace: mirai
  labels:
    app: mirai-diagram
spec:
  type: ClusterIP
  selector:
    app: mirai-diagram
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
