apiVersion: v1
kind: Service
metadata:
  name: embedding-service
  namespace: mirai
  labels:
    app: embedding-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: embedding-service
