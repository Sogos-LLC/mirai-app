# Redis service for dev environment
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: redis-local-dev
  labels:
    app: redis
    environment: dev
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    name: redis
  selector:
    app: redis
