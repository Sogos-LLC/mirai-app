apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: mirai-local-dev
  labels:
    app: ai-service
    environment: dev
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: ai-service
