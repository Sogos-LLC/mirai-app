---
# Frontend: mirai.local
# Routes to the main Mirai application (Next.js frontend)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mirai-frontend
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`mirai.local`)
      kind: Rule
      services:
        - name: mirai-frontend
          port: 80
      middlewares:
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# Marketing Site: get-mirai.local
# Routes to the marketing/landing page (Next.js marketing frontend)
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mirai-marketing
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`get-mirai.local`)
      kind: Rule
      services:
        - name: mirai-marketing
          port: 80
      middlewares:
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# Backend API: api.mirai.local
# Routes to the Connect-RPC backend service
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mirai-backend
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`api.mirai.local`)
      kind: Rule
      services:
        - name: mirai-backend
          port: 8080
      middlewares:
        - name: api-cors
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# Kratos Public API: auth.mirai.local
# Routes to Ory Kratos authentication service (public endpoint)
# Kratos is deployed via Helm to mirai namespace
# The service name follows the pattern: <release-name>-public
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kratos-public
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`auth.mirai.local`)
      kind: Rule
      services:
        # Kratos Helm chart creates service named "<release>-public"
        - name: kratos-public
          port: 80
      middlewares:
        - name: kratos-cors
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# Mailpit: mailpit.mirai.local
# Routes to the Mailpit email testing UI
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mailpit
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`mailpit.mirai.local`)
      kind: Rule
      services:
        - name: mailpit
          port: 8025
      middlewares:
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# MinIO Console: minio.mirai.local
# Routes to the MinIO web console for storage management
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: minio-console
  namespace: mirai-local
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`minio.mirai.local`)
      kind: Rule
      services:
        - name: minio
          port: 9001
      middlewares:
        - name: security-headers
  tls:
    secretName: mirai-tls

---
# HTTP to HTTPS redirect for all hosts
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: http-redirect
  namespace: mirai-local
spec:
  entryPoints:
    - web
  routes:
    - match: HostRegexp(`{host:.+}`)
      kind: Rule
      services:
        # Dummy service - will never be reached due to redirect
        - name: mirai-frontend
          port: 80
      middlewares:
        - name: https-redirect
