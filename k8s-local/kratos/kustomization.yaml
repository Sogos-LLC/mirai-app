apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mirai-local

resources:
  - mailpit.yaml
  - secrets.yaml

# Note: Kratos itself is installed via Helm, not Kustomize
# Install with: helm install kratos ory/kratos -f values-local.yaml -n mirai-local
