apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: mirai-uat
  labels:
    app: ai-service
    environment: uat
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: ai-service
