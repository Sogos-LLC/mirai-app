# Embedding service for internal cluster access
---
apiVersion: v1
kind: Service
metadata:
  name: embedding
  namespace: mirai-uat
  labels:
    app: embedding
    environment: uat
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  selector:
    app: embedding
